# dogfood-ui レポート — 2026-05-22

**対象**: Marp スライドビューア / fit（全体表示）+ トラックパッド ピンチズーム・パン
**URL**: http://localhost:8064（root: `/tmp/mdv-presenter-bug/`）
**検証デッキ**: `deck.md`（42 枚・テキスト）/ `imgtest.md`（4 枚・フルブリード画像 `![bg]`）
**手段**: Playwright MCP（実 UI クリック / 実キー入力 / 実ダブルクリック）+ DOM 幾何計測

> 計測方針: 本機能はレイアウトが本質なので、PASS 判定は **「active SVG の bounding rect が
> スライドペインの 4 辺すべてに収まる（`fitsNow`）」** という幾何条件で行う。これはスクショ目視より
> 厳密。なお MCP スクショはサーバー側サンドボックスに保存され当環境の FS からは読めないため、
> 証跡は本レポートの計測値（数値）とする。

## 集計
- ✅ PASS: 9 件
- ⚠️ PARTIAL: 1 件（焦点追従の過渡ドリフト・許容）
- ❌ FAIL: 0 件
- ⏸️ SKIP: 0 件

## 検証項目

| # | 項目 | 操作 | 計測結果 | 判定 |
|---|------|------|----------|:--:|
| T1 | **fit / 横長ペイン（報告バグ）** | ノート枠展開・1900×820・ペインaspect 3.29 | SVG 802×451 が領域 1614×491 に 4 辺収まる | ✅ |
| T2 | **fit / フルブリード画像スライド** | `imgtest.md` の `![bg]` 画像スライドへ実Nextで移動 | SVG 802×451 fits / 画像 `/raw/notebooklm/slide-02.png` は 200・image/png・2.18MB で配信 | ✅ |
| T3a | **ピンチズームイン** | ctrl+wheel（pinch open）×7 | 802→2827px に拡大、ペインがスクロール可能に | ✅ |
| T3b | **パン** | ズーム後 `scrollTop += 150` | SVG が画面上で -150px 移動（ネイティブ overflow スクロール＝二本指パン） | ✅ |
| T3c | **ズーム/パンの分離** | ctrl なし wheel | サイズ不変（縮尺は変わらず、二本指スクロールはパン専用） | ✅ |
| T4 | **キーボードズーム** | 実キー `=` ×2 | 802×1.25²≈1253px、`marp-zoomed` 付与 | ✅ |
| T5 | **ダブルクリックで fit に戻す** | 実ダブルクリック（`.marp-slide-area`） | `marp-zoomed` 解除・inline px クリア | ✅ |
| T6 | **スライド送りでズーム解除** | 実キー ArrowRight（ズーム中） | 4/4 へ遷移・ズーム解除・scrollTop 0 | ✅ |
| T7a | **ペインリサイズ追従（fit時）** | ノート枠 240→460px に拡大 | スライドペイン 271px でも SVG が収まる（411×231 fits） | ✅ |
| T7b | **ペインリサイズ追従（ズーム中）** | ズーム中にノート枠 460→120px | ResizeObserver が fit を再計算（705→1742px に再追従） | ✅ |
| — | **キーボード `0` で fit** | 実キー `0`（ズーム中） | ズーム解除・クリア | ✅ |
| — | **フルスクリーン fit + ズーム + 復帰** | F → ズーム → 終了 | フルスクリーン fit、ズーム/パン可、終了で fit 復帰 | ✅ |

### ⚠️ PARTIAL: 焦点追従の過渡ドリフト
- ピンチの焦点（カーソル位置）は、コンテンツがペインより小さい **fit付近→拡大の過渡区間**で
  中央寄せの影響により最大 ~13% ドリフトする。コンテンツがペインを超えた後（拡大済み領域）は正確に追従。
- これは「カーソル基準ズーム＋中央 fit」を両立する標準UX（Preview.app / ブラウザ / Figma 系）で
  共通の挙動で、fit から拡大していく一瞬だけの軽微なもの。許容と判断。

## 回帰
- ノート枠ドラッグ（`MarpSplitHandle`）: 既存どおり動作。リサイズ後も fit が追従（T7a/b）。
- コンソールエラー: 全セッションで **0 件**（既存の Tailwind CDN 警告のみ）。
- 自動テスト: `npm test` 293 件全 PASS（既存 281 + 新規 `test-marp-zoom.js` 12）。

## 結論
報告された「Marp 表示でスライド／画像が上下に見切れる」バグは、横長ペイン（報告と同条件）・縦長ペイン・
画像スライドの全てで **完全に収まる**ことを実機計測で確認。トラックパッド ピンチズーム＋二本指パンも実機で動作。
**デプロイ可（push/publish はユーザー GO 待ち）**。
